{{- $pctx := . -}}
{{- if .IsHome -}}{{ $pctx = .Site }}{{- end -}}
{{- $pages := slice -}}
{{- if or $.IsHome $.IsSection -}}
{{- $pages = $pctx.RegularPages -}}
{{- else -}}
{{- $pages = $pctx.Pages -}}
{{- end -}}
{{- $limit := .Site.Config.Services.RSS.Limit -}}
{{- if ge $limit 1 -}}
{{- $pages = $pages | first $limit -}}
{{- end -}}
{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ if eq  .Title  .Site.Title }}{{ .Site.Title }}{{ else }}{{ with .Title }}{{.}} on {{ end }}{{ .Site.Title }}{{ end }}</title>
    <link>{{ .Permalink }}</link>
    <description>Recent content {{ if ne  .Title  .Site.Title }}{{ with .Title }}in {{.}} {{ end }}{{ end }}on {{ .Site.Title }}</description>
    <generator>Hugo</generator>{{ with .Site.LanguageCode }}
    <language>{{.}}</language>{{end}}{{ with .Site.Copyright }}
    <copyright>{{.}}</copyright>{{end}}{{ if not .Date.IsZero }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{ end }}
    {{ with .OutputFormats.Get "RSS" -}}
    {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{ end -}}
    {{ range $pages }}
    <item>
      <title>{{ .Title }}</title>
      {{- if eq .Params.post_type "link" -}}
      {{- with .Params.link_url -}}
      <link>{{ . }}</link>
      {{- else -}}
      <link>{{ .Permalink }}</link>
      {{- end -}}
      {{- else -}}
      <link>{{ .Permalink }}</link>
      {{- end }}
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      <guid>{{ .Permalink }}</guid>
      <description>
      {{- $content := .Content -}}
      {{- /* Remove div wrapper from YouTube embeds and style iframe directly */ -}}
      {{- $content = $content | replaceRE `<div class="youtube">\s*` "" -}}
      {{- $content = $content | replaceRE `\s*</div>` "" -}}
      {{- /* Add inline styles to iframe for proper display in RSS readers */ -}}
      {{- $content = $content | replaceRE `<iframe` `<iframe style="max-width:100%; display:block; margin:1em 0;"` -}}
      {{- $content | html -}}
      {{- /* Add permalink for link posts */ -}}
      {{- if eq .Params.post_type "link" -}}
      &lt;p&gt;&lt;a href=&quot;{{ .Permalink }}&quot;&gt;Permalink&lt;/a&gt;&lt;/p&gt;
      {{- end -}}
      </description>
    </item>
    {{ end }}
  </channel>
</rss>
